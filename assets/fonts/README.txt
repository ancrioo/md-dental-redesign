Place any custom web fonts here (woff / woff2 / ttf).

This project uses Google Fonts by default (Inter). To use local fonts:
1. Add the font files into this folder.
2. Update assets/css/global.css to include @font-face declarations.
